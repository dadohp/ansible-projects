---
- hosts: 192.168.100.215
  remote_user: root
  tasks:
    - name: install Samba Service
      yum:
          name: samba
          state: present
    - name: stop Samba Service
      service:
          name: "{{ item }}"
          state: started
      with_items:
          - 'smb'
          - 'nmb'
...
